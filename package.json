{
  "name": "autogtm",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:inngest": "npx inngest-cli@latest dev",
    "build": "turbo run build",
    "start": "npm run start --workspace=autogtm",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "npm@10.8.2",
  "dependencies": {
    "@supabase/supabase-js": "2.109.0"
  }
}
